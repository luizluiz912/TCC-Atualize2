<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.tcc.rubira.tcc_projeto1">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:debuggable="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="HardcodedDebugMode">
        <activity android:name=".Activitys.UsuariosActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activitys.startActivity"
            android:label="@string/title_activity_start" />
        <activity android:name=".Activitys.ClientesActivity" />
        <activity android:name=".Activitys.consultar_excluir_usuarioActivity" />
        <activity android:name=".Activitys.cadastrar_usuarioActivity" />
        <activity android:name=".Activitys.cadastro_servicoActivity" />
        <activity android:name=".Activitys.cadastro_clienteActivity" />
        <activity android:name=".Activitys.adicionar_servicoActivity" />
        <activity android:name=".Activitys.detalhes_orcamentoActivity" />
        <activity android:name=".Activitys.servicos_telaActivity" />
        <activity android:name=".Activitys.loginActivity" />
        <activity android:name=".Activitys.consultar_excluir_clientesActivity" />

    </application>

</manifest>
